# Railway: backend NestJS + Prisma
# Si en Railway el "Root Directory" del servicio es Backend-BoysRoofing, este archivo se usa.

[build]
builder = "nixpacks"

[deploy]
startCommand = "sh scripts/railway-start.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
